// Valores thuthy y falsy (verdadero o falso)

//falsos
Boolean(0); //false;

Boolean(null); //false;

Boolean(NaN); //false;

Boolean(undefined); //false;

Boolean(""); //false;

// Verdaderos
Boolean("a"); // true

Boolean(1); //true

Boolean([]); //true cualquier array

Boolean({}); //true cualquier objeto

Boolean(function () {}); //true cualquier funcion

Boolean(true); //true
