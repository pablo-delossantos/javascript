// Qué es JavaScript?
// Surge de la necesidad de interacción con los sitios web.
// Es un lenguaje interpretado, orientado a objetos, debilmente tipado y dinámico.

4 + "7"; // 47
4 * "7"; // 28
2 + true; // 3
false - 3; // -3

// Dos etapas: compilacion y runtime
// Cuando compilamos donde se traduce del lenguaje a binario.
// JavaScript corre directamente en el runtime: cuando lo abrimos en el navegador corre automaticamente (a diferencia de Java o C++).

// Realmente es un lenguaje interpretado?

// Backwards y forwards
// JS es backward compatible: todas las nuevas versiones o módulos se pueden utilizar. Para eso tenemos que usar el compilador Babel para usar las nuevas funciones en nuestro entorno de trabajo.
